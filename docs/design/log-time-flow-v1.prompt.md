# Log time flow v1 image prompt

Mode: built-in image generation, reference-image edit
Use case: `ui-mockup`
Source: `today-concept-v3.png`
Output: `log-time-flow-v1.png`

> Preserve the two side-by-side light and dark Android phones, their Today
> screen, top-right settings and profile actions, and the exact bold monochrome
> Luqa design language. Show the result immediately after tapping `+ Log time`:
> a tall modal bottom sheet is open on both phones over a subtly dimmed Today
> timeline.
>
> The sheet contains a centered drag handle; `Log time` with a close action;
> `Today · Wednesday, 27 August`; the persistent field `What did you do?` with
> value `Writing thesis`; Category with a purple identity mark and selected
> value `Master thesis`; equal Start `09:00` and End `11:45` fields; derived
> duration `2h 45m`; and a Recent list containing `Writing thesis` / `Master
> thesis`, `Lunch` / `Food`, and `Gym` / `Training`. The fixed bottom action is
> `Add entry`, black in light mode and white in dark mode.
>
> Use crisp 12–16 dp sheet corners, flat surfaces, thin structural dividers,
> high-density comfortable spacing, tabular time figures, strong contrast, and
> 48 dp targets. Recent activities are list rows, not chips or cards.
>
> Do not add a giant timer, Stop button, keyboard, date calendar, clock dial,
> floating-card stack, gradients, glassmorphism, shadows inside fields, or extra
> navigation destinations. Do not merge category and description. Preserve
> Settings and Profile. Render a high-fidelity 1536x1024 product UI board.
